"""Generators of the images that are derived from the design tokens.

The build imports this package (`from tools import gen_assets`); the scripts
in it can also be run directly, e.g. `python tools/gen_assets.py`.
"""
